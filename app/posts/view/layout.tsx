// The article chrome lives one level up so both the statically generated
// /posts/<slug> route and the legacy /posts/view redirect render inside the
// same header and background. See app/posts/article-shell.tsx.
export { default } from "../article-shell";
